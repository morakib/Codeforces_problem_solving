//array e te permutation--> distinct element tai oi index ta obossoi ekbar thakbe 
//tai oi index tai iteraton +1 korbo
//last e just minimum ber korbo
//An inversion in an array a is a pair of indices (i,j) such that i>j and (ai<aj)
#include<bits/stdc++.h>
using namespace std;
int main()
{   int t;
	cin>>t;
	while(t--){
	int n;
    cin>>n;
    int a[n];   
    int b[n];
	for(int i=1;i<=n;i++)
	{
	    cin>>a[i];  
        b[a[i]]=i+1;     
    }
	for(int i=1;i<=n;i++)
        cout<<min(b[i],n)<<" ";
	cout<<"\n";
}
}

